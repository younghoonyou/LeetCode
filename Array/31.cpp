#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
class Solution{
public:
    void DFS(int k,vector<int> temp){
        if()
            for (int i = k;i<;++i){
                if()
            }
    }
    void nextPermutation(vector<int> &nums){
        vector<int> temp = nums;
        sort(temp.begin(),temp.end());
        DFS(0,nums,temp);
    }
};
/*
    constraints use constant extra memory

    1 <= nums.length <= 100

    0 <= nums[i] <= 100
*/
int main(){}